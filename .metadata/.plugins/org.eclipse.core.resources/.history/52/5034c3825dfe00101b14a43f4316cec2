<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Shopping Cart</title>

    <style>
        body {
            font-family: Arial, sans-serif;
            background: #f5f5f5;
        }
        .container {
            width: 80%;
            margin: 30px auto;
            background: white;
            padding: 20px;
            border-radius: 8px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            padding: 12px;
            text-align: center;
            border-bottom: 1px solid #ddd;
        }
        img {
            width: 80px;
        }
        .qty-btn {
            padding: 4px 10px;
            background: #232f3e;
            color: white;
            text-decoration: none;
            border-radius: 4px;
        }
        .total {
            text-align: right;
            font-size: 20px;
            margin-top: 20px;
        }
    </style>
</head>

<body>

<div class="container">
    <h2>Your Cart</h2>

    <table>
        <thead>
            <tr>
                <th>Image</th>
                <th>Product</th>
                <th>Price</th>
                <th>Qty</th>
                <th>Total</th>
                <th>Action</th>
            </tr>
        </thead>

        <tbody>
            <tr th:each="item : ${cartItems}">
               <td>
    <img th:src="@{'http://localhost:8085/uploads/images/' + ${item.imgurl}}"
         alt="Product"
         style="width:80px;">
</td>

                <td th:text="${item.title}"></td>
                <td th:text="${item.price}"></td>
                <td th:text="${item.quantity}"></td>
                <td th:text="${item.totalPrice}"></td>
                <td>
                    <a th:href="@{/cart/increase/{id}(id=${item.productId})}" class="qty-btn">+</a>
                    <a th:href="@{/cart/decrease/{id}(id=${item.productId})}" class="qty-btn">-</a>
                </td>
            </tr>
        </tbody>
    </table>

    <div class="total">
        <strong>Grand Total: ₹ <span th:text="${grandTotal}"></span></strong>
    </div>
</div>

</body>
</html>
