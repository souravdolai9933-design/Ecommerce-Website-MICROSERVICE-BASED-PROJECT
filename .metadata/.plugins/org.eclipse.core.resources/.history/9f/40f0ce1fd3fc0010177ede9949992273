<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>E-Commerce Store</title>

    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
        }

        /* HEADER */
        .header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            background-color: #232f3e;
            padding: 12px 20px;
            color: white;
        }

        .logo {
            font-size: 22px;
            font-weight: bold;
        }

        .search-bar input {
            width: 350px;
            padding: 8px;
            border-radius: 4px;
            border: none;
        }

        .header-buttons button {
            margin-left: 10px;
            padding: 8px 14px;
            border: none;
            background-color: #febd69;
            cursor: pointer;
            font-weight: bold;
            border-radius: 4px;
        }

        /* LAYOUT */
        .container {
            display: flex;
        }

        /* SIDEBAR */
        .sidebar {
            width: 220px;
            background-color: white;
            padding: 15px;
            border-right: 1px solid #ddd;
        }

        .sidebar h3 {
            margin-bottom: 10px;
        }

        .category {
            padding: 8px;
            cursor: pointer;
            color: #007185;
            text-decoration: none;
            display: block;
        }

        .category:hover {
            background-color: #f0f0f0;
        }

        /* PRODUCTS */
        .product-area {
            flex: 1;
            padding: 20px;
        }

        .products {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
            gap: 20px;
        }

        .product-card {
            background-color: white;
            padding: 15px;
            border-radius: 6px;
            box-shadow: 0 0 5px rgba(0,0,0,0.1);
        }

        .product-card img {
            width: 100%;
            height: 160px;
            object-fit: contain;
        }

        .product-card h4 {
            margin: 10px 0 5px;
        }

        .price {
            font-weight: bold;
            color: green;
        }

        .product-card button {
            width: 100%;
            margin-top: 10px;
            padding: 8px;
            border: none;
            background-color: #ffa41c;
            font-weight: bold;
            cursor: pointer;
            border-radius: 4px;
        }
    </style>
</head>

<body>

<!-- ================= HEADER ================= -->
<div class="header">
    <div class="logo">MyShop</div>

    <!-- SEARCH -->
    <form class="search-bar" th:action="@{/user/all}" method="get">
        <input type="text" name="name" placeholder="Search products..." />
    </form>

    <div class="header-buttons">
        <button>Menu</button>
        <button>Cart</button>
    </div>
</div>

<!-- ================= MAIN CONTENT ================= -->
<div class="container">

    <!-- CATEGORY SIDEBAR -->
    <div class="sidebar">
        <h3>Categories</h3>

        <!-- All Products -->
        <a th:href="@{/user/home}" class="category">
            All Products
        </a>

         
    </div>

    <!-- PRODUCT LIST -->
    <div class="product-area">
        <h2>Products</h2>

        <!-- Empty State -->
        <p th:if="${#lists.isEmpty(products)}">
            No products found.
        </p>

        <div class="products">
            <div class="product-card" th:each="product : ${products}">
                <img th:src="@{/images/{img}(img=${product.imgurl})}" />

                <h4 th:text="${product.name}"></h4>
                <p th:text="${product.title}"></p>

                <p class="price">
                    ₹ <span th:text="${product.price}"></span>
                </p>

                <button>Order Now</button>
            </div>
        </div>
    </div>

</div>

</body>
</html>
