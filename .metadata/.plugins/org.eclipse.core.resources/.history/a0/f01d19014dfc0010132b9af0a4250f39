server:
  port: 3333

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true

      routes:
          uri: lb://ecommerce-backend
          predicates:
            - Path=/api/product/**,/api/product_category/**
