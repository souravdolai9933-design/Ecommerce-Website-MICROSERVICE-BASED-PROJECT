server:
  port: 3333

spring:
  application:
    name: API-GATEWAY

  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true

      routes:
        - id: ecombackend-service
          uri: lb://ecommerce-backend
          predicates:
            - Path=/api/product/**,/api/product_category/**
